# Test data

`content-cases.json` is the initial, versioned fixture contract. Cases represent content emitted by the self-hosted demo SPA and include expected truth labels. They must not contain data copied from third-party sites.
